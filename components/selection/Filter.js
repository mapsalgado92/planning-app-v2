import { useState } from "react"
/**
 *
 * @param {
 * items: Object []
 * styles: Object
 * } param0
 * @returns
 *
 * item: {
 *      name: String,
 *      payload: Object
 * }
 *
 * styles: {
 *      button: String (className),
 *      display: String (className),
 * }
 */

const Filter = ({ items, styles }) => {
  const [selected, setSelected] = useState([])

  return <div></div>
}

export default Filter
